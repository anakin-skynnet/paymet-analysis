# Dependencies for Databricks Apps. Apps have fastapi, uvicorn, databricks-sdk pre-installed.
# Only list packages to add or override. Use binary wheels only (no psycopg source) to avoid build failures.
pydantic-settings>=2.11.0,<3
sqlmodel>=0.0.27
psycopg[binary,pool]>=3.2
