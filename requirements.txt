# Databricks App: only add packages not pre-installed (fastapi, uvicorn, databricks-sdk are already there).
# Keep minimal and use names that resolve to wheels to avoid "error installing packages".
# psycopg[binary] = main psycopg package + C binary; required for postgresql+psycopg:// (do not use psycopg-binary alone).
pydantic-settings
sqlmodel
psycopg[binary]
